<Window x:Class="SMART_ERP.Views.AccountsReceivableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:SMART_ERP.ViewModels"
        mc:Ignorable="d"
        Title="Cuentas por Cobrar" 
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F5F5F5">
    
    <Window.DataContext>
        <viewmodels:AccountsReceivableViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" 
                   Text="Cuentas por Cobrar" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#2196F3"
                   Margin="0,0,0,20"/>

        <!-- Resumen -->
        <Border Grid.Row="1" 
                Background="White" 
                Padding="20" 
                CornerRadius="10" 
                BorderThickness="1" 
                BorderBrush="#E0E0E0"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Pendiente (Todas)" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalBalance, StringFormat=L {0:N2}}" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="#2196F3"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Total Vencido" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalOverdue, StringFormat=L {0:N2}}" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="#F44336"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Error Message -->
        <TextBlock Grid.Row="2" 
                   Text="{Binding ErrorMessage}" 
                   Foreground="#D32F2F" 
                   FontSize="12"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Tabla de Cuentas -->
        <Border Grid.Row="3" 
                Background="White" 
                CornerRadius="10" 
                BorderThickness="1" 
                BorderBrush="#E0E0E0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0" 
                          ItemsSource="{Binding Accounts}" 
                          SelectedItem="{Binding SelectedAccount}"
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          BorderThickness="0"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E0E0E0"
                          RowBackground="White"
                          AlternatingRowBackground="#F9F9F9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Factura" Binding="{Binding InvoiceNumber}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding CustomerName}" Width="2*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=L {0:N2}}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Pagado" Binding="{Binding PaidAmount, StringFormat=L {0:N2}}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Saldo" Binding="{Binding Balance, StringFormat=L {0:N2}}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Vencimiento" Binding="{Binding DueDate, StringFormat=dd/MM/yyyy}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Días Vencido" Binding="{Binding DaysOverdue}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Botones de Acción -->
                <StackPanel Grid.Row="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            Margin="0,15,0,0">
                    <TextBlock Text="Monto Pago:" Margin="0,0,10,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding PaymentAmount, StringFormat=N2}" Width="120" Padding="8" Margin="0,0,10,0"/>
                    <Button Content="Registrar Pago" 
                            Command="{Binding RecordPaymentCommand}" 
                            Width="120" 
                            Height="35" 
                            Margin="0,0,10,0"
                            Background="#4CAF50" 
                            Foreground="White" 
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                   CornerRadius="5" 
                                                   Padding="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45A049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="Actualizar" 
                            Command="{Binding RefreshCommand}" 
                            Width="100" 
                            Height="35" 
                            Background="#2196F3" 
                            Foreground="White" 
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                   CornerRadius="5" 
                                                   Padding="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1976D2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
